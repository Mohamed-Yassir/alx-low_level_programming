#include "main.h"

/**
 * print_square - print a square followed by a new line
 * @size: the number and size  of the square
 * Return: empty
 */

void print_square(int size)

{
	int x, z;

	if (size <= o)
	{
		_putchar('\n');
	}
	else
	{
	for (x = 0; x < size; x++)
	{
	for (z = 0; z < size; z++)
	{
	_putchar(35);
	}
	_putchar('\n');
	}
	}
}
