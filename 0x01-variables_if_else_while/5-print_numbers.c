#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/*
 * main - Print all single digits of base 10 starting from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int a = 0;

	for (a = 0; a <= 9; a++);
	printf("%d\n", a);

	return (0);
}

